fastapi>=0.115.2
uvicorn>=0.32.0
pydantic>=2.9.2
pydantic-settings>=2.6.1
google-cloud-pubsub>=2.26.1
google-cloud-firestore>=2.19.0
google-cloud-storage>=2.18.2
google-cloud-run>=0.10.12
# google-cloud-aiplatform / vertexai version-lock each other internally with
# == pins. Floor + major-ceiling lets Dependabot upgrade within the 1.x line.
# See commits ddf1daa..08ed41e for history.
google-cloud-aiplatform>=1.88.0,<2
vertexai>=1.43.0,<2
httpx>=0.27.2
PyGithub>=2.4.0
slack-sdk>=3.33.1
# In gcp/aws mode the scheme executor's run_lint / run_tests nodes shell out to
# the detected stack's commands on this host (handlers._run_on_host). For a
# Python target that is `python -m pytest` and `python -m ruff check .`, so
# these two must exist in the image or every Python task fails its CI node.
pytest>=8.0
ruff>=0.15.4,<0.16

"""Offline evaluation harness for Henchmen (BYO-LLM parity checks).

Lives inside the installed package so ``henchmen eval`` works from any
working directory, not just a repo checkout. The fixtures, baseline and
methodology notes stay at ``evals/`` in the repo as data.
"""

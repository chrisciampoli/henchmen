"""Henchmen offline evaluation fixtures and baseline (repo data).

The runnable harness lives in :mod:`henchmen.evals`; ``evals.harness`` and
``evals.storage`` remain as thin shims so a repo checkout keeps working.
"""
